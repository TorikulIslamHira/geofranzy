package com.geofrenzy.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================
// GeoFrenzy Color Palette
// Dark Mode First — Vibrant Blue/Purple/Pink
// ============================================

// --- Primary (Deep Blue) ---
val Blue80 = Color(0xFF7BC8FF)
val Blue60 = Color(0xFF4DA6FF)
val Blue40 = Color(0xFF0078FF)
val BlueDark = Color(0xFF003D80)

// --- Secondary (Purple Glow) ---
val Purple80 = Color(0xFFB589FF)
val Purple60 = Color(0xFF9A5CFF)
val Purple40 = Color(0xFF7B2FFF)

// --- Accent (Pink/Magenta) ---
val Pink80 = Color(0xFFFF80C8)
val Pink60 = Color(0xFFFF4DB3)
val Pink40 = Color(0xFFE0007A)

// --- SOS Red ---
val SOSRed = Color(0xFFFF3B30)
val SOSRedDark = Color(0xFF8B0000)

// --- Surfaces (Dark Mode) ---
val Background = Color(0xFF0A0E1A)      // Near-black navy
val SurfaceDark = Color(0xFF111827)     // Deep dark card
val SurfaceMedium = Color(0xFF1E2A3A)   // Elevated surface
val SurfaceLight = Color(0xFF2A3A50)    // Lighter card

// --- Glass Effect ---
val GlassSurface = Color(0x40FFFFFF)    // White at 25% — glassmorphism
val GlassBorder = Color(0x20FFFFFF)     // White at 12%

// --- Text ---
val TextPrimary = Color(0xFFE8F0FF)     // Near-white with blue tint
val TextSecondary = Color(0xFF8A9DC0)   // Muted blue-grey
val TextDisabled = Color(0xFF3A4A60)

// --- Status ---
val OnlineGreen = Color(0xFF00E676)
val OfflineGrey = Color(0xFF616161)
val WarningAmber = Color(0xFFFFAB00)
