# =============================================
# Friend Locator — Environment Variables
# Copy this file to .env and fill in values
# ALL SERVICES BELOW ARE 100% FREE
# =============================================

# --- Server ---
PORT=5000
NODE_ENV=development

# --- MongoDB Atlas (Free Tier) ---
# Sign up at: https://www.mongodb.com/cloud/atlas
# Create a free cluster and paste the connection string below
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/friend-locator?retryWrites=true&w=majority

# --- JWT (JSON Web Tokens) ---
# Make this a long, random string (for security)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=30d

# --- OpenWeatherMap (Free Tier: 1000 calls/day) ---
# Sign up at: https://openweathermap.org/api
# Free plan is sufficient for this app
OPENWEATHER_API_KEY=your_openweathermap_api_key

# --- Google OAuth (Free) ---
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# --- Proximity Settings ---
# Distance in meters to trigger "You are near a friend!" notification
NEARBY_THRESHOLD_METERS=500

# --- Frontend URL (for CORS) ---
# Change this to your deployed frontend URL in production
CLIENT_URL=http://localhost:3000
