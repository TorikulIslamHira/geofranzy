{
  "name": "geofranzy-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Geofranzy",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@sentry/node": "^10.39.0",
    "@sentry/tracing": "^7.120.4",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "22"
  }
}
